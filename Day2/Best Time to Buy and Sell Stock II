class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        m=0
        nums=prices
        for i in range(len(nums)-1):
            if(nums[i+1]>nums[i]):
                m+=nums[i+1]-nums[i]
        return m
